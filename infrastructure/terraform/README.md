# Terraform Placeholder

Production infrastructure will include PostgreSQL, Redis, S3-compatible object storage, CDN, queue workers, and observability.

This directory is intentionally reserved for Phase 4 so the app can evolve locally before cloud resources are committed.
